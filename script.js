//your JS code here. If required.
function browsweDetect{
	let x = navigator.userAgent;
	let y = navigator.appName;
	let z = navigator.appVersion;
	document.getElementById("browser-info").innerHTML = "You are using " + y + " version " + z.
}

	